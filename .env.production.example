# Alto Central - Production Environment Variables
# Copy this file to .env and fill in your values
#
# IMPORTANT: Never commit .env files with real credentials to version control

# ==============================================================================
# REQUIRED - Application Security
# ==============================================================================

# Secret key for JWT/session encryption (generate with: openssl rand -hex 32)
SECRET_KEY=your-secure-secret-key-here

# PostgreSQL password for local database
POSTGRES_PASSWORD=your-secure-postgres-password

# ==============================================================================
# OPTIONAL - API Keys
# ==============================================================================

# API key for external clients (optional, for API authentication)
API_KEY=

# Anthropic Claude API key (for AI analytics features)
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# OpenAI API key (alternative LLM provider)
OPENAI_API_KEY=

# ==============================================================================
# APPLICATION SETTINGS
# ==============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# CORS origins (comma-separated, adjust for your domain)
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ==============================================================================
# FEATURE FLAGS
# ==============================================================================

# Enable machine learning features (requires additional dependencies)
ENABLE_ML=false

# Enable optimization algorithms
ENABLE_OPTIMIZATION=false

# Enable LLM-based features (chat, AI analytics)
ENABLE_LLM=true

# ==============================================================================
# NOTES
# ==============================================================================
#
# External database connections (TimescaleDB, Supabase, MongoDB) are configured
# in config/sites.yaml on a per-site basis. This stack only manages:
#   - Local PostgreSQL (for app data, ML models, chat history)
#   - Redis (for caching and task queue)
#
# To generate secure keys:
#   SECRET_KEY: openssl rand -hex 32
#   POSTGRES_PASSWORD: openssl rand -base64 24
#
# SSL/TLS certificates should be placed in nginx/ssl/ directory:
#   - fullchain.pem
#   - privkey.pem
